// Workspace settings
{
    // The following will hide the js and map files in the editor
    "files.exclude": {
        "**/__pycache__": true,
        ".pytest_cache": true,
        ".mypy_cache": true,
    }
}
